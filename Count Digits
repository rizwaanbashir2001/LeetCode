int countDigits(int n){
	// Write your code here.
int count=0,temp=n;
    while(n)
    {
        int k=n%10;
        if( k!=0 && temp%k==0)
         count++;
        n/=10;
    }   
    return count;
	
}
